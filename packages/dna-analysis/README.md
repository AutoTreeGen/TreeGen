# dna-analysis

Phase 6 пакет (DNA analysis service). Сейчас **placeholder** — реальная
реализация в Phase 6 (см. `ROADMAP.md` §10).

Существует как workspace-член только чтобы `uv` мог разрешать `packages/*`
без ошибок. Не импортировать в код.
